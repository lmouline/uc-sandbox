package ldas.duc.language.test;

import org.junit.Test;

import java.io.IOException;
import java.net.URISyntaxException;

public class TestLanguage {
    private static final String LUC_LANG = "luc";

    @Test
    public void testSimplePrint() throws URISyntaxException, IOException {
//        File file = new File(this.getClass().getResource("/add.luc").toURI());
//
//        Source source = Source.newBuilder(LUC_LANG, file).build();
//        Context context = Context.newBuilder(LUC_LANG).in(System.in).out(System.out).build();
//
//        Value result = context.eval(source);
//        if (!result.isNull()) {
//            System.out.println(result.toString());
//        }

    }
}
